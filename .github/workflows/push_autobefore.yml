name: 定时推送Before

on:
  schedule:
    - cron: '0 0 0 1 * ?'   #Runs

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: run start.sh
        run: |
          bash ./start.sh

name: 每月4日7点推送

on:
  schedule:
    - cron: '0 7 4 * ?'   #Runs

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: run start.sh
        run: |
          python ./main.py ${{ secrets.PUSH_TOKEN }}
